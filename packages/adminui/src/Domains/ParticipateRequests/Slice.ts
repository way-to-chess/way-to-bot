import { createSlice } from "@reduxjs/toolkit";

const participateRequestsSlice = createSlice({
  name: "participateRequests",
  initialState: {},
  reducers: {},
  extraReducers: (builder) => {},
});

export { participateRequestsSlice };
