export enum EEventStatus {
  WAITING = "waiting",
  STARTED = "started",
  FINISHED = "finished",
}

export enum EUserRole {
  ADMIN = "admin",
  USER = "user",
}

export enum EImageAssigment {
  AVATAR = "avatar",
}

export enum ECsvAssigment {
  ROUNDS = "rounds",
  RATING = "rating",
}
