import { TEXT } from "@way-to-bot/shared/constants/text";

const DEFAULT_PADDING = { padding: 16 };

const REQUIRED_RULES = [{ required: true, message: TEXT.common.requiredField }];

export { DEFAULT_PADDING, REQUIRED_RULES };
